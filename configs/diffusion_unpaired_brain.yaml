# Diffusion Unpaired Brain (Cycle-Diffusion CT -> MRI)
model:
  architecture: "diffusion_unpaired"
  region: "brain"
  base_model: "stabilityai/stable-diffusion-2-1-base"
  lora_rank_unet: 16 # Often higher for unpaired
  lora_rank_vae: 8
  input_nc: 3
  output_nc: 3

training:
  epochs: 100
  batch_size: 1
  lr: 0.00005 # Slower for stability
  mixed_precision: "fp16"
  lambda_cycle: 1.0

data:
  image_size: 512
  unpaired: true
  prompt_source: "CT scan"
  prompt_target: "MRI scan"
